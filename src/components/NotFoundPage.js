import React from "react";
import { Link } from "react-router-dom";

const NotFoundPage = () => (
  <div>
    404! - <Link to="/">Ir a página principal</Link>
  </div>
);

export default NotFoundPage;
